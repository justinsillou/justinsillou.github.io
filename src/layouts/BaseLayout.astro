---
import "../styles/global.css";
import Clarity from "@microsoft/clarity";
import Header from "../components/Header";
import Footer from "../components/Footer.astro";
const { pageTitle } = Astro.props;

//import SnowParticles from "../components/SnowParticles.jsx";
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <link rel="icon" href="/favicon.svg" />
  </head>
  <body
    class="bg-background text-text leading-relaxed min-h-screen grid grid-rows-[auto_1fr_auto]"
  >
    <!-- Header -->
    <Header client:load />

    <!-- Contenu principal -->
    <main class="px-4 max-w-7xl mx-auto w-full">
      <slot />
    </main>

    <!-- Footer -->
    <Footer />

    <!-- <SnowParticles client:only="preact" /> -->

    <script type="text/javascript">
      (function (c, l, a, r, i, t, y) {
        c[a] =
          c[a] ||
          function () {
            (c[a].q = c[a].q || []).push(arguments);
          };
        t = l.createElement(r);
        t.async = 1;
        t.src = "https://www.clarity.ms/tag/" + i;
        y = l.getElementsByTagName(r)[0];
        y.parentNode.insertBefore(t, y);
      })(window, document, "clarity", "script", "txxydspury");
    </script>
  </body>
</html>
